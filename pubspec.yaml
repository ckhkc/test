name: test
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter
  flutter_map: ^7.0.2  # Check for the latest version
  latlong2: ^0.9.1    # For coordinate handling
  geocoding: ^3.0.0
  pointer_interceptor: ^0.10.1+2
  routing_client_dart: ^0.5.5
  http: ^1.3.0
  provider: ^6.1.4
  flutter_rating_bar: ^4.0.1
  

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
flutter:
  uses-material-design: true
  assets:
    - assets/black_map_pin.png
    - assets/flat_design_map_pin.png
    - assets/orange_map_pin.png